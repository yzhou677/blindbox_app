# PAUSED — not used in production. Mercari live internal test is taken down; gateway runs fixture.
# Official marketplace API (eBay Browse) is the production priority. Code path retained for future experiments.
#
# To re-enable internal live test only:
#   cp env.live.internal.example .env.blindbox-collection
#   (add MERCARI_EXTRA_HEADERS_JSON if needed) && npm run deploy
#
MERCARI_GATEWAY_MODE=live
MERCARI_ACQUISITION_RUNTIME=fetch
MERCARI_GATEWAY_DEBUG=1
# MERCARI_EXTRA_HEADERS_JSON={"cookie":"..."}  # bridge for fetch runtime only
# MERCARI_ACQUISITION_RUNTIME=playwright       # future — not implemented
